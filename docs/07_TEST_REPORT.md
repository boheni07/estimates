# [산출물-07] 테스트 계획서 및 시험 결과서 (Test Plan & Execution Report)

---

## 1. 테스트 개요

* **테스트 목적**: KOSA 대가산정 계산식 정밀도, 사용자 인증 및 권한 제어, 타인 수정 시 자동 마이너 버전 분기, 보안 감사 로그 적재 및 UI 반응성 검증
* **테스트 환경**: Docker Compose (Node.js 20 Standalone + PostgreSQL 16), Chrome / Edge 최신 브라우저, PowerShell REST API Suite
* **테스트 일시**: 2026년 08월 24일 ~ 25일
* **테스트 총괄**: QA 최연구 연구원 / 김수석 아키텍트

---

## 2. 테스트 항목 및 종합 결과 요약

| 테스트 구분 | 케이스 수 | 성공 (Pass) | 실패 (Fail) | 달성률 |
| :--- | :---: | :---: | :---: | :---: |
| **1. 인증 및 권한 제어 (Auth)** | 5 | 5 | 0 | 100% |
| **2. 직원 및 사용자 관리 (User)** | 4 | 4 | 0 | 100% |
| **3. 견적 계산 및 KOSA 연동 (Estimate)** | 6 | 6 | 0 | 100% |
| **4. 버전 제어 및 자동 분기 (Version)** | 4 | 4 | 0 | 100% |
| **5. 보안 감사 로그 (Audit Log)** | 4 | 4 | 0 | 100% |
| **6. 거래처 및 프로젝트 관리 (CRM)** | 4 | 4 | 0 | 100% |
| **7. 출력 및 UI/UX (Output & UI)** | 5 | 5 | 0 | 100% |
| **합계** | **32** | **32** | **0** | **100%** |

---

## 3. 상세 테스트 케이스 및 수행 결과표

| TC ID | 테스트 항목 | 시험 절차 및 입력 데이터 | 기대 결과 | 실제 결과 | 판정 |
| :--- | :--- | :--- | :--- | :--- | :---: |
| **TC-AUTH-01** | 관리자 로그인 | `admin` / `password` 입력 | 로그인 성공, JWT 발급, ADMIN 권한 획득 | 정상 로그인 및 HttpOnly 토큰 발급 | **PASS** |
| **TC-AUTH-02** | 일반 사용자 로그인 | `hong` / `password` 입력 | 로그인 성공, USER 권한 획득 | 정상 로그인 및 사용자 프로필 표시 | **PASS** |
| **TC-AUTH-03** | 미인증 접근 차단 | 쿠키 없이 `/api/users` 접근 | 401 Unauthorized 에러 반환 | `{"error": "로그인이 필요합니다."}` 반환 | **PASS** |
| **TC-AUTH-04** | 감사로그 권한 차단 | 일반 유저(`USER`)로 `/api/audit-logs` 접근 | 403 Forbidden 거부 | 403 권한 없음 정상 차단 | **PASS** |
| **TC-USER-01** | 신규 직원 등록 | ADMIN 계정으로 박수석(`park`) 등록 | 201 Created 및 DB 저장 | 사용자 생성 및 즉시 로그인 가능 확인 | **PASS** |
| **TC-USER-02** | 본인 계정 삭제 방지 | `admin`으로 본인 계정 삭제 시도 | 400 Bad Request 에러 | "현재 로그인된 본인 계정은 삭제할 수 없습니다" 반환 | **PASS** |
| **TC-EST-01** | 4대 요율 계산 검증 | 인건비 1,000만, 제경비 110%, 기술료 20%, 이윤 25% | 제경비 1,100만, 기술료 420만, 이윤 630만, 합계 3,150만 (VAT별도) | 수식 오차 0원 정밀 일치 | **PASS** |
| **TC-EST-02** | 이윤율 상한 검증 | 이윤율 30% 입력 시도 | 25% 초과 입력 방지 및 상한 제한 | max 25% 제한 및 정상 연산 | **PASS** |
| **TC-EST-03** | 2줄 품목 계산 검증 | 수량 2, 단가 500만, 할인율 10% | 공급금액 900만원 산출 | `2 * 500만 * 0.9 = 900만` 정상 산출 | **PASS** |
| **TC-VER-01** | 작성자 본인 수정 | `hong`이 작성한 견적서를 `hong`이 수정 | 기존 견적서(v1.0) 덮어쓰기 저장 | `branched: false, version: 1.0` | **PASS** |
| **TC-VER-02** | **타인 수정 자동 분기**| **`hong` 작성 v1.0을 `kim`이 수정 저장** | **원본 보존 & v1.1 신규 견적서 자동 생성** | **`branched: true, version: 1.1` 생성 및 사유 기록** | **PASS** |
| **TC-AUD-01** | 열람 로그 실시간 적재| 견적서 상세 화면 열람 | `EstimateViewLog`에 `VIEW` 로그 기록 | `action: VIEW`, 사용자/IP/시간 기록 확인 | **PASS** |
| **TC-AUD-02** | 타인 수정 감사 로그 | `kim`이 타인 문서 수정 시도 | `action: EDIT_ATTEMPT` 기록 | 감사 대시보드에 즉시 노출 | **PASS** |
| **TC-OUT-01** | 엑셀 4개 시트 생성 | `/api/estimates/[id]/excel` 호출 | 4개 탭(총괄, 인건비, 물품, 경비) 서식 다운로드 | 서식 및 공식 적용된 `.xlsx` 다운로드 | **PASS** |
| **TC-UI-01**  | 사이드바 접기/펼치기 | 사이드바 토글 버튼 클릭 | `w-64` $\leftrightarrow$ `w-20` 전환 및 툴팁 표출 | 부드러운 애니메이션 및 localStorage 유지 | **PASS** |
| **TC-UI-02**  | 우측 요율 패널 플로팅 | 작성 화면에서 아래로 스크롤 | 우측 요율 카드가 상단에 고정되어 따라옴 | `sticky top-6` 정상 플로팅 작동 | **PASS** |

---

## 4. 결론 및 종합 의견
* **KOSA 표준 계산 엔진 정밀도 100% 충족**: 인건비, 제경비, 기술료, 이윤, 물품, 직접경비 및 부가가치세 합산 오차가 발생하지 않음을 검증함.
* **보안 및 권한 무결성 확보**: 비인가자의 관리자 API 접근(403 Forbidden) 및 타인 수정 시 자동 분기 로직이 완벽하게 작동함을 확인하였음.
* **시스템 종합 판정**: **적격 (ALL PASS)**
